<section class="content-header">
  <h1>
    <%= @note.name %>
  </h1>

  <ol class="breadcrumb">
    <li>
      <%= link(to: dashboard_path(@conn, :index)) do %>
        <i class="fa fa-dashboard"></i> Home
      <% end %>
    </li>
    <li><%= link("Notes", to: note_path(@conn, :index)) %></li>
    <li><%= link(@note.name, to: note_path(@conn, :show, @note.id)) %></li>
    <li class="active">Edit</li>
  </ol>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <%= render "_form.html", path: note_path(@conn, :update, @note.id), changeset: @changeset, title: "Edit", submit: "Update" %>
    </div>
  </div>
</section>
