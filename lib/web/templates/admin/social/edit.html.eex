<section class="content-header">
  <h1>
    <%= @social.name %>
  </h1>

  <ol class="breadcrumb">
    <li>
      <%= link(to: dashboard_path(@conn, :index)) do %>
        <i class="fa fa-dashboard"></i> Home
      <% end %>
    </li>
    <li><%= link("Socials", to: social_path(@conn, :index)) %></li>
    <li><%= link(@social.name, to: social_path(@conn, :show, @social.id)) %></li>
    <li class="active">Edit</li>
  </ol>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <%= render "_form.html", path: social_path(@conn, :update, @social.id), changeset: @changeset, title: "Edit", submit: "Update" %>
    </div>
  </div>
</section>
