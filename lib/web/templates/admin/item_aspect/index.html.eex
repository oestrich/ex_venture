<section class="content-header">
  <h1>
    Item Aspects
  </h1>

  <ol class="breadcrumb">
    <li>
      <%= link(to: dashboard_path(@conn, :index)) do %>
        <i class="fa fa-dashboard"></i> Home
      <% end %>
    </li>
    <li class="active">Item Aspects</li>
  </ol>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools">
            <%= link("New Item Aspect", to: item_aspect_path(@conn, :new), class: "btn btn-primary") %>
          </div>
        </div>
        <div class="box-body">
          <div class="table-responsive">
            <table class="table">
              <thead>
                <tr>
                  <th>ID</th>
                  <th>Name</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <%= Enum.map @item_aspects, fn (item_aspect) -> %>
                  <tr>
                    <td><%= item_aspect.id %></td>
                    <td><%= link(item_aspect.name, to: item_aspect_path(@conn, :show, item_aspect.id)) %></td>
                    <td>
                      <%= link("View", to: item_aspect_path(@conn, :show, item_aspect.id), class: "btn btn-default btn-xs") %>
                      <%= link("Edit", to: item_aspect_path(@conn, :edit, item_aspect.id), class: "btn btn-default btn-xs") %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
